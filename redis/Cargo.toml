[package]
name = "redis"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1.37.0", features = ["full"] }
bytes = "1.6.0"
async-recursion = "1.1.1"

[dev-dependencies]
rstest = "0.19.0"
